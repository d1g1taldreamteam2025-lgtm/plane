---
import { getDict, type Lang } from "../i18n";
interface Props { lang: Lang; }
const { lang } = Astro.props;
const items = getDict(lang).marquee;
const loop = [...items, ...items, ...items, ...items];
---
<section aria-hidden="true" class="border-y border-[var(--color-line)] bg-[var(--color-plum-900)] py-5 overflow-hidden">
  <div class="marquee-track flex w-[400%] gap-6 whitespace-nowrap font-display text-base font-semibold tracking-widest text-white sm:gap-12 sm:text-xl">
    {loop.map((txt, i) => (
      <span class="flex items-center gap-6 sm:gap-12">
        <span class={i % 2 === 0 ? "text-[var(--color-gold-400)]" : "text-[var(--color-blush-300)]"}>✦</span>
        <span class="uppercase">{txt}</span>
      </span>
    ))}
  </div>
</section>
