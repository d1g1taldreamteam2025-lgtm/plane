; Force this folder to behave like a standalone pnpm project.
; Prevents Vercel (and any clean pnpm install) from treating it
; as part of the monorepo workspace at /plane/pnpm-workspace.yaml.
ignore-workspace=true
